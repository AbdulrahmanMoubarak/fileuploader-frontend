import { SystemTicketModel } from './system-ticket-model';

describe('SystemTicketModel', () => {
  it('should create an instance', () => {
    expect(new SystemTicketModel()).toBeTruthy();
  });
});
